{
  "name": "marp-presenter",
  "version": "0.1.0",
  "private": true,
  "description": "A custom presenter console for Marp/Marpit decks.",
  "scripts": {
    "watch": "marp --theme-set themes --server .",
    "pdf": "marp --theme-set themes --allow-local-files --pdf"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2"
  }
}
